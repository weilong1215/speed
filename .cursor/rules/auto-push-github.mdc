---
description: 修正後自動 commit 並 push 到 GitHub
alwaysApply: true
---

# 自動同步 GitHub

此專案已連結遠端：`https://github.com/weilong1215/speed.git`（branch: `main`）。

完成程式修正後，除非使用者另有指示，應自動：

1. 只 commit 與修正相關的檔案（例如 `main.py`），不要納入 `debug*.py`、`test*.py` 等本機測試腳本，除非使用者明確要求。
2. 撰寫簡潔的 commit message，說明修正原因。
3. 若遠端有新 commit，先 `git pull --rebase origin main` 再 push。
4. Push 到 `origin main`。

若 push 因認證失敗而無法完成，告知使用者並提供已完成的 commit hash。
